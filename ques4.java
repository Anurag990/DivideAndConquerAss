package DivideAndConquer;import java.util.*;public class ques4 {    public static String checkPermutationSum(int[] a, int[] b, int k) {        int n = a.length;        // Sort array 'a' in ascending order        Arrays.sort(a);        // Sort array 'b' in descending order        Arrays.sort(b);        reverseArray(b);        // Check if the sum of corresponding elements is greater than or equal to k        for (int i = 0; i < n; i++) {            if (a[i] + b[i] < k) {                return "No";            }        }        return "Yes";    }    private static void reverseArray(int[] arr) {        int start = 0;        int end = arr.length - 1;        while (start < end) {            int temp = arr[start];            arr[start] = arr[end];            arr[end] = temp;            start++;            end--;        }    }    public static void main(String[] args) {        int[] a1 = {2, 1, 3};        int[] b1 = {7, 8, 9};        int k1 = 10;        System.out.println(checkPermutationSum(a1, b1, k1));  // Output: Yes        int[] a2 = {1, 2, 2, 1};        int[] b2 = {3, 3, 3, 4};        int k2 = 5;        System.out.println(checkPermutationSum(a2, b2, k2));  // Output: No    }}