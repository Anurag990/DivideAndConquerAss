package DivideAndConquer;import java.util.*;public class ques5 {    public static boolean checkIntervalIntersection(int[][] intervals) {        // Sort the intervals based on start times        Arrays.sort(intervals, (a, b) -> Integer.compare(a[0], b[0]));        // Check for overlapping intervals        for (int i = 1; i < intervals.length; i++) {            if (intervals[i][0] <= intervals[i - 1][1]) {                // Overlapping intervals found                return true;            }        }        // No overlapping intervals found        return false;    }    public static void main(String[] args) {        int[][] intervals1 = {{1, 3}, {5, 7}, {2, 4}, {6, 8}};        System.out.println(checkIntervalIntersection(intervals1));  // Output: Yes        int[][] intervals2 = {{1, 3}, {7, 9}, {4, 6}, {10, 13}};        System.out.println(checkIntervalIntersection(intervals2));  // Output: No    }}